# Session handoff — 2026-08-22

## Summary

dolphinRust ends synchronized with `origin/main` at `be07c21` after PR #51 merged and issue #50
closed. The engine now applies one native-grid layover/shadow mask per active burst before
covariance and phase linking, propagates explicit validity through every processing path, and
rejects unsupported public-config values before workflow I/O. The exact merged tree passed CI.
GitHub reports zero open dolphinRust issues and zero open pull requests.

This is merged source, not a release or GroundPulse deployment. The latest release and
`eo/origin/main` submodule pin remain v1.5.0 at `8a63fe2`. GroundPulse has not populated the new
mask field or produced a production receipt for `be07c21`. The implementation tree was clean
before this EOD write; only the canonical and dated local handoffs are changed afterward.

## Completed

- Refreshed the live queue and confirmed issue #50 was the only open dolphinRust issue. Its two
  engine requirements were recorded in the committed intake and plan; GroundPulse adoption was
  explicitly deferred rather than treated as shipped.
- Commit `21b6a73` established the red contract and pinned dolphin 0.35.0 at upstream
  `e567e554300f9bb2c6c4c49358d41876ce81e5a7` as the oracle. The focused test failed before
  production code because the masked sequential API and explicit validity output did not exist.
- Commit `c6355ba` implemented issue #50:
  - one single-band native-grid GTiff per burst, mapped by OPERA burst ID;
  - fail-closed missing, duplicate, extra, unparseable, misaligned, partially covered, or changed
    inputs;
  - finite-nonzero validity with nodata and GDAL-invalid exclusion, applied after raw-data SHP
    selection and before covariance/phase linking;
  - explicit validity through strides, tiles, bounded runs, multiburst stitching, references,
    corrections, quality, displacement, uncertainty, velocity, resumable state, and incremental
    state; the later unwrap `mask_file` remains independent;
  - exhaustive disposition of all 99 public config paths: 30 consumed, 42 conditional, and 27
    compatibility-only, with unsupported non-defaults rejected before workflow I/O;
  - corrected the pinned preprocessing interpolation-threshold default to `0.3`.
- Commit `06fc6c2` recorded the implementation receipt, oracle provenance, focused gates, config
  coverage, and downstream boundary.
- PR #51 merged as `be07c2141e0e53ef761991f1ffba29b928d8296b`; issue #50 closed `COMPLETED`.
  The merge changed 29 files (`+4,805/-258`) and added the config-disposition suite, mask contract,
  reproducible oracle generator, and seven fixtures.
- Cleaned the local repository after merge: fast-forwarded `main`, removed the isolated issue #50
  worktree, pruned stale worktree metadata, and retained only the main worktree. The prior local
  August 18 handoffs remain recoverable in `stash@{0}`.

## In progress

- No dolphinRust implementation or pull request is in progress. The merged topic branch refs were
  retained; no branch deletion was requested.
- Issue #50 remains under `Unreleased`. No tag or release points at `be07c21`; v1.5.0 at `8a63fe2`
  is still the latest GitHub Release.
- GroundPulse integration remains deferred. `eo/origin/main` still pins `8a63fe2`, does not populate
  `layover_shadow_mask_files`, and its checked-in real-dolphin YAML sets
  `worker_settings.threads_per_worker: 6`, which the new config guard rejects. eo#475 tracks engine
  option adoption; eo#477 tracks the customer-facing mask/correction claims. eo#419 remains a
  separate open terminal-pipeline blocker.
- No production-shaped OPERA STATIC mask integration, large-scene peak-RSS canary, engine release,
  GroundPulse pin, deploy, or same-run terminal-artifact verification was performed.
- Deferred work from the August 18 handoff was not resumed: no 2018 L4 metered probe/cohort fetch
  was performed, and crates.io or binary publication was not performed or verified in this
  session. `RELEASING.md` remains unchanged from the prior handoff and requires its identified
  corrections before a crates.io publication path is used.

## Verification

Passing evidence:

- The implementation receipt records the focused aligned-mask, config, layover/shadow,
  displacement, multiburst, sequential, SHP, resumable, and incremental contracts passing.
- The receipt also records `cargo fmt --all -- --check`, `cargo check --workspace`, strict workspace
  Clippy, `cargo test --workspace` including 109/109 workflow unit tests, Python compileall, 33/33
  validation tests, and `git diff --check` passing locally before the PR.
- PR-head CI run `32597910783` passed on `06fc6c2`.
- Post-merge main CI run `32602108406` passed formatting, workspace check, strict all-target Clippy,
  Rust tests, and Python validation on exact merge SHA `be07c21`. The workflow's existing 384-date
  GPU deterministic stress test remained explicitly skipped.
- Fresh `git fetch --prune origin` reconciliation produced `main...origin/main = 0 0`; GitHub
  returned zero open issues and zero open PRs; only the main worktree remains.
- The canonical and dated EOD files were checked byte-identical with `cmp -s`, and their patch
  passed `git diff --check`.

Not established:

- No local suite was rerun after merge; the exact merged-tree evidence is GitHub CI run
  `32602108406`.
- No new release artifact, crates.io package, binary, GroundPulse submodule update, deployment,
  production-shaped peak-RSS result, terminal artifact, or scientific acceptance was established
  or verified in this session.
- GitHub Actions emitted a Node 20 deprecation annotation for `actions/checkout@v4` and
  `actions/setup-python@v5`; it did not fail this run.

## Open questions

1. When should the issue #50 change be versioned and released for GroundPulse consumption?
2. Should eo#475 and eo#477 jointly own `GP-050-TRUTH`, or should the dedicated integration issue
   named in the intake be created before release/pin work starts?
3. Is the one-granule metered L4 probe still the next authorized atmospheric-validation action?
   The full cohort fetch remains outside the approved work completed here.
4. Should the local `.codex` handoff chain be committed for fresh-clone visibility, or remain local
   under the existing convention?

## Next actions

1. Start with `$briefing` and refresh Git/GitHub state; there is currently no open dolphinRust
   implementation backlog.
2. If issue #50 is selected for release, choose the version, update release artifacts, and verify
   the exact tag before any GroundPulse pin.
3. In `eo`, normalize the incompatible worker setting, extract/map OPERA STATIC masks per burst,
   populate the engine config under eo#475, and reconcile the limitations text under eo#477.
4. After the release and pin, run a production-shaped STATIC integration, bounded large-scene RSS
   canary, deployment checks, and a fresh correlated terminal-artifact verifier. Keep each result
   distinct.
5. If crates.io publication or atmospheric validation is selected instead, resume from the
   August 18 handoff in `stash@{0}` and recheck every point-in-time assumption first.

## References

- Branch/upstream: `main` synchronized with `origin/main` at
  `be07c2141e0e53ef761991f1ffba29b928d8296b`; divergence `0 0`.
- Issue #50: `fix(config): layover_shadow_mask_files is inert — declared, defaulted, never read`.
- PR #51: `fix: apply layover shadow masks before phase linking`.
- Contract, implementation, and receipt commits: `21b6a73`, `c6355ba`, `06fc6c2`.
- Key artifacts: `md/intake/issue-50-layover-shadow-mask-2026-08-22.md`,
  `md/plans/issue-50-layover-shadow-mask-2026-08-22.md`, `CHANGELOG.md`,
  `crates/dolphin-core/src/config.rs`, and `crates/dolphin-workflows/src/displacement.rs`.
- CI: PR run `32597910783`; post-merge main run `32602108406`.
- Latest release and downstream pin: v1.5.0 / `8a63fe2`; live `eo/origin/main` pin checked after
  fetch. No `eo` working-tree files were changed.
- Downstream trackers: eo#475, eo#477, and the separate eo#419 terminal-pipeline blocker.
- Previous handoff: August 18 canonical and dated copies preserved in
  `stash@{0} On main: codex: preserve local handoffs before PR 51 merge`.
- This EOD write is local; no handoff commit or push was requested.
