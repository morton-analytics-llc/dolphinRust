# Session handoff — 2026-07-25

## Summary

dolphinRust ends the session synchronized with `origin/main` at `bb79b4e`, with no open issues or
pull requests. The combined reliability and uncertainty work, CI repair, and tiled non-finite
coverage fix all merged through PRs #17–#19. GroundPulse completed fresh bounded production
acceptance and closed dolphinRust #10/#11 with correlated terminal-artifact receipts.

## Completed

- PR #17 (`6fb8014`) added unwrap connected components, coherence-derived L2/velocity weighting,
  posterior/residual/velocity uncertainty outputs, and regenerable MMX1/ICMX calibration artifacts.
- PR #18 (`d0b71f9`) repaired Ubuntu HDF5 discovery and software-Vulkan CI coverage.
- PR #19 (`bb79b4e`) preserved nodata through tiled phase linking and removed the production
  all-non-finite failure mechanism.
- GroundPulse T137 acceptance completed phase linking, two-burst stitching, unwrap, inversion,
  corrections, velocity, writing, COG, distinct phase-linking-coherence COG, and PMTiles.
- Peak RSS was 2,483,532 KiB (about 2.37 GiB), below 5.5 GiB; no exit 139/OOM occurred.
- The exact correlated terminal-artifact verifier passed and repeat dispatch reused the terminal
  run without duplicate compute.
- Issues #10–#16 are closed; there are no open dolphinRust issues or PRs.

## In progress

- No dolphinRust code is in progress.
- Weighted MMX1/ICMX empirical coverage remains `not_evaluable` because the ICMX primary window
  has only 5/25 finite CRLB-weighted pixels. This is not a known code defect.
- GroundPulse water-mask policy still suppresses public map publication for the accepted artifact;
  this boundary was not bypassed.

## Verification

- Full local Rust/Python/oracle/real-data gates passed for PR #17.
- GitHub Actions runs `30138907433` and `30166301446` passed.
- Production receipt records provenance `/3`, 48 total tiles, 32 linked, 16 nodata, 235,400 valid
  of 235,404 pixels, bounded RSS, normal exit, correlated artifacts, and verifier success.
- `main...origin/main = 0 0` at `bb79b4e`.

## Open questions

1. Open a research/data issue for the `not_evaluable` weighted ICMX coverage boundary?
2. Remove merged auxiliary worktrees/branches in a later cleanup pass?
3. Commit the `.codex` handoff chain or keep it local?

## Next actions

1. Start with `$briefing`; the repo has no open implementation backlog.
2. If calibration is prioritized, obtain a weighted-finite ICMX window and regenerate reliability
   artifacts without lowering the 50% finite-pixel threshold.
3. Keep later GroundPulse workers and water-mask/public-serving work in the GroundPulse session.

## References

- `main`/`origin/main`: `bb79b4ee60ef6c33fe09bacc076cbdc8a164b62d`.
- PRs: #17 (`6fb8014`), #18 (`d0b71f9`), #19 (`bb79b4e`).
- GroundPulse acceptance head: `1f97f4c8`.
- CI: `30138907433`, `30166301446`.
- Previous archive: `.codex/handoffs/2026-07-21.md`.
