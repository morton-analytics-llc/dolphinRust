# Session handoff — 2026-08-23

## Summary

PR #55 merged at `da9116c` and established the v1.6 conditional-uncertainty boundary. Issue #52
is implemented on `codex/issue-52-global-covariance` at `8eb413b`; PR #56 is open, non-draft,
`CLEAN`, `MERGEABLE`, and green. It remains unmerged because the approved backlog workflow stops
before merge. Issue #52 therefore remains open. Ryan's next action is the manual merge decision.

The #52 implementation adds opt-in, byte-capped `sequential_source_dag_v1` capture/replay for
same-pixel temporal covariance across ministacks, with an exact acquisition-0 gauge, streamed
SHA-256-bound HDF5 artifacts, provenance/factor receipts, source-identity checks, and fail-closed
resource and replay gates. Artifacts remain `uncalibrated` and
`blocked_pending_issue_54_and_53`. No release, GroundPulse/velocity wiring, submodule bump,
deployment, production terminal artifact, or scientific calibration was established.

The root checkout remains at `be07c21`, four commits behind `origin/main`, because its local
handoff/intake/plan changes were preserved. The clean #52 implementation worktree is based on
current `origin/main` at `da9116c`. This EOD write is local and uncommitted.

## Completed

- Refreshed the live GitHub queue and recorded issues #52, #53, and #54 in
  `md/intake/open-issues-uncertainty-2026-08-23.md` with complete dispositions and an ordered plan
  in `md/plans/open-issues-uncertainty-2026-08-23.md`.
- Merged prerequisite PR #55 as `da9116c689d1b61c0ea8a9d145de6a57ffc28563`; main CI run
  `32667672347` passed on that exact merge SHA.
- Implemented issue #52 contract-first in an isolated worktree. The branch contains the analytic
  source-DAG contract, source-influence capture, canonical streamed artifact schema, exact replay,
  deterministic manifest binding, fail-closed identity/resource validation, focused docs, and
  release smoke coverage.
- Hardened the implementation against stale or mismatched manifests, changed source/model
  identity, invalid replay topology, cap bypasses, unsupported producer scope, and partial
  artifact finalization.
- Fixed the only PR CI failure in `8eb413b` by deriving the Faer workspace receipt at runtime;
  stable component and exact cap-composition assertions remain fixed. The failure was test
  portability, not covariance algebra or resource enforcement.
- Opened PR #56, `feat: add bounded sequential covariance replay`, with `automation-pr`; it closes
  #52 when merged.
- Independent final science/resource review found no unresolved blocker or acceptance-gate
  finding for #52's declared producer scope.

## In progress

- PR #56 is the only open pull request. It is open, non-draft, `CLEAN`, `MERGEABLE`, and green at
  `8eb413b3d111a7d6d983f81226c90251d4268c1f`; it has not been merged.
- Issue #52 remains open until PR #56 merges. The isolated worktree at
  `/private/tmp/dolphinrust-issue52.N22LF4` is clean and retained.
- Issues #54 and #53 remain separate human gates and were not implemented. The required order is
  #52 producer -> #54 spatial-reference covariance -> #53 temporal-slope calibration.
- CLI artifacts remain `source_model_unavailable`; replayable low-level artifacts require a
  verified external proper-complex source resolver/model.
- Supported #52 scope is full-batch CPU/f64, Rect support, `AlwaysFirst`, reference 0, single burst,
  and microbatch 1. Resumable replay, target/reference subtraction, and multiburst seam covariance
  remain out of scope.
- The local intake and plan still describe their original planning snapshot, including PR #55 as
  draft and implementation not started. They are preserved uncommitted and should be reconciled
  before any decision to commit them.
- The root checkout and the two prerequisite topic worktrees were retained; no cleanup or branch
  deletion was requested.

## Verification

Passing evidence:

- PR #56 CI run `32683884440`, job `97305275773`, passed formatting, workspace check, strict
  Clippy, Rust tests, and Python validation on exact head `8eb413b`.
- After the portability fix, the focused exact-cap contract passed locally, followed by
  `cargo check --workspace --all-targets`,
  `cargo clippy --workspace --all-targets -- -D warnings`, and the exact CI Rust command
  `cargo test --workspace -- --skip gpu_emi_deterministic_384`.
- Before the test-only portability fix, `cargo fmt --all -- --check`,
  `cargo test --workspace -- --test-threads=1`, and the release smoke passed.
- The 435-source production replay rejected cap-minus-one before source reads and accepted the
  exact cap. The 26-date, 8x8, two-block release smoke produced a finite 26x26 covariance with an
  exact gauge, a 194,116-byte HDF5 artifact, an 8,969,231-byte dependency/provider receipt, and
  40,648,704-byte peak RSS.
- Fresh GitHub reconciliation shows one open PR (#56) and three open issues (#52-#54). PR #55 and
  main CI run `32667672347` are complete and green at `da9116c`.

Not established:

- PR #56 has not merged, issue #52 has not closed, and no post-merge combined-tree CI exists for
  the #52 change.
- The smoke is deterministic synthetic resource evidence for one spatial block, not scientific
  validation, multi-tile/K-saturation proof, or production serving evidence.
- The per-query cap excludes Faer's process-global cold-start runtime state; it is not a
  total-cold-process heap bound.
- No #54 target/reference covariance, #53 calibrated temporal inference, engine release,
  GroundPulse pin, deploy, or correlated terminal-artifact receipt exists for this change.

## Open questions

1. Will Ryan manually merge PR #56 now that its exact head is green and mergeable?
2. Should the uncommitted intake/plan and local handoff chain be reconciled and committed after the
   merge, or remain local?
3. After #56 and combined-tree CI are complete, is #54 the next approved implementation item?
4. When should the retained implementation and prerequisite worktrees/topic branches be removed?

## Next actions

1. Manually merge PR #56 if approved; the automation workflow must not perform the merge.
2. Refresh `main`, verify issue #52 closes, and require green combined-tree main CI at the merge
   SHA before treating #52 as complete.
3. Reconcile the local intake/plan against the merged state. Preserve #54 and #53 as separate
   human-approved, contract-first work with #54 before #53.
4. Do not expose inferential or calibrated uncertainty, publish a release, bump GroundPulse, or
   claim production evidence until the named downstream gates have their own receipts.
5. Clean retained worktrees/branches only after confirming no local-only evidence is needed.

## References

- PR #55: https://github.com/morton-analytics-llc/dolphinRust/pull/55
- Main prerequisite merge and CI: `da9116c689d1b61c0ea8a9d145de6a57ffc28563`, run
  `32667672347`.
- Issue #52: https://github.com/morton-analytics-llc/dolphinRust/issues/52
- PR #56: https://github.com/morton-analytics-llc/dolphinRust/pull/56
- PR #56 head and CI: `8eb413b3d111a7d6d983f81226c90251d4268c1f`, run
  `32683884440`, job `97305275773`.
- Issue #52 branch commits: `3e1b352` through `8eb413b`; design at
  `md/design/sequential-global-covariance.md`.
- Issues #53 and #54: https://github.com/morton-analytics-llc/dolphinRust/issues/53 and
  https://github.com/morton-analytics-llc/dolphinRust/issues/54.
- Latest release remains v1.5.0; no release action was taken.
- Previous handoff: `.codex/handoffs/2026-08-22.md`.
- This EOD write is local; no handoff/intake/plan commit or push was requested.
