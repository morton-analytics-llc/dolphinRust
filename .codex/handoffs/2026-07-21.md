# Session handoff — 2026-07-21

## Summary

Closed the remaining dolphinRust issue queue by implementing and publishing a genuine
phase-linking coherence artifact, matching the pinned dolphin v0.35.0 all-nonfinite input
behavior, and rerunning the stale 64-pixel throughput benchmark. The reviewed implementation is
committed and pushed to `main` at `0713ca4`; the branch is synchronized with `origin/main`, the
implementation tree is clean, and there are no open PRs or issues. Only this local handoff update
remains uncommitted.

## Completed

- Re-benchmarked the 64-pixel native unwrap policy on the 1024x1024, seven-interferogram harness:
  native 3.4 s wall / 23.7 CPU s / 543 MiB versus SNAPHU 21.4 s / 123.9 CPU s / 453 MiB. Peak
  actual concurrency throughput was 1,521 versus 227 frames/hour (6.7x). These are isolated local
  synthetic measurements, not production capacity proof.
- Implemented issue #7's bounded per-date coherence-matrix magnitude calculation while the fused
  matrix is resident. Recorded that dolphin v0.35.0's public `avg_coh` is an argmax date index,
  while its internal `abs(C).mean(axis=3)` is the genuine bounded intermediate used here.
- Implemented issue #9's opt-in `phase_linking_coherence.tif`, distinct from estimator-fit
  `temporal_coherence.tif`, through sequential, tiled, multiburst, GPU-staged, and incremental NRT
  paths. Compressed-SLC pseudo-dates are excluded and uneven ministacks use real-date weighting.
- Added optional displacement/provenance fields and bumped geometry provenance to schema `/2`,
  method `2.0.0`. The layer is controlled by `phase_linking.calc_average_coh`, default `false` for
  upstream-compatible artifact behavior.
- Resolved issue #8 with a pinned dolphin v0.35.0 oracle: an acquisition containing only NaNs is
  rejected with `PhaseLinkRuntimeError`; dolphinRust now rejects all-nonfinite acquisitions before
  phase linking.
- Reviewed the implementation and fixed a redundant CPU validation scan plus a stale provenance
  design assertion before the final verification pass.
- Updated `CHANGELOG.md`, `PLAYBOOK.md`, `VALIDATION.md`, the idea-scout ledger, oracle helpers,
  and `md/plans/open-issues-resolution-2026-07-21.md`.
- Committed and pushed `0713ca4 feat(phaselink): emit distinct coherence artifact`, closing issues
  #7, #8, and #9.

## In progress

- No dolphinRust code is in progress. Only `.codex/session-handoff.md` and the new dated archive
  are local/uncommitted.
- GroundPulse/eo integration is running separately and was not changed from this repository. It
  must update its dolphinRust reference, enable `phase_linking.calc_average_coh: true`, accept
  provenance schema `/2`, and consume `phase_linking_coherence.tif` without aliasing temporal
  coherence.
- The coherence resource A/B is a local synthetic receipt: at 1x1 sampling the optional layer
  added about 67.4 MiB peak RSS and 0.042 s wall time. No 60 GiB terminal or production-shaped
  GroundPulse run was performed here.

## Verification

Completed and passing at `0713ca4`:

- Red-to-green analytic, oracle, fused/staged, sequential, NRT, tiled/whole, multiburst,
  end-to-end raster, GPU configuration, and provenance contracts.
- `cargo test --workspace`, including GPU contracts and the local live MMX1 contract.
- `cargo fmt --all -- --check`.
- `cargo check --workspace`.
- `cargo clippy --all-targets --workspace -- -D warnings`.
- `oracle/.venv/bin/python oracle/check_all_nan_v035.py` returned the expected pinned
  `PhaseLinkRuntimeError` rejection.
- Python compile checks and `git diff --check`.
- Post-push reconciliation: local HEAD and `origin/main` both resolve to
  `0713ca4ef017a9b3e39669612d939dd8d49f8835`; GitHub reports no open issues or PRs.

Not run:

- GroundPulse worker/integration tests, deployment, `scripts/pipeline-verified.sh`, correlated DB
  lineage, object existence, or serving proof.
- A production-shaped or 60 GiB terminal memory/concurrency validation of the new optional layer.
- A new atmospheric-corrected MMX1/ICMX GNSS comparison; the prior provisional validation boundary
  remains unchanged.

## Open questions

1. Should GroundPulse enable the new layer in every production run or only workflows that consume
   the coherence value? The recommended current boundary is library-default off, eo-production on.
2. After eo integration, does the production-shaped memory receipt remain within its worker
   envelope, or does aggregation need further containment?
3. The earlier policy question remains open: should provisional MMX1/ICMX GNSS thresholds become
   hard release gates after atmospheric correction and additional station-pair coverage?

## Next actions

1. In `../eo`, update the dolphinRust reference to `0713ca4` and enable
   `phase_linking.calc_average_coh: true` in the intended production configuration.
2. Update GroundPulse ingestion/provenance consumers for `phase_linking_coherence.tif` and schema
   `/2`; do not substitute `temporal_coherence.tif`.
3. Run eo's focused worker contracts, complete deployment, and then run
   `scripts/pipeline-verified.sh` for lineage, artifact, and serving proof.
4. Capture a production-shaped memory/runtime receipt with the layer enabled and compare it with
   the local 1x1 A/B before making capacity claims.
5. Start the next dolphinRust session with `$briefing`; there is currently no repo-local backlog.

## References

- Branch/upstream: `main` synchronized with `origin/main` at `0713ca4`.
- Commit: `0713ca4 feat(phaselink): emit distinct coherence artifact`.
- Issue state: #7, #8, and #9 closed; no open issues or PRs.
- Implementation plan: `md/plans/open-issues-resolution-2026-07-21.md`.
- Validation receipts: `VALIDATION.md` and `PLAYBOOK.md`.
- Metric kernel: `crates/dolphin-phaselink/src/quality.rs` and `fused.rs`.
- Workflow/output: `crates/dolphin-workflows/src/sequential.rs`, `displacement.rs`, and
  `provenance.rs`.
- Pinned all-NaN oracle: `oracle/check_all_nan_v035.py`.
- Previous handoff: `.codex/handoffs/2026-07-14.md`; this handoff supersedes its stale 64-pixel
  re-benchmark action.
