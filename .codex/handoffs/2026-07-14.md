# Session handoff — 2026-07-14

## Summary

Merged the scheduled covariance follow-up PR, executed the first full MMX1/ICMX independent
GNSS gate, fixed the native MCF final-epoch cycle slip that gate exposed, and landed a reviewed
follow-up. The corrected native and SNAPHU runs both pass the provisional GNSS bars. The
implementation head is synchronized with `origin/main` at `9fec550`; the local branch adds only
this EOD handoff commit. There are no open PRs or issues.

## Completed

- Reviewed and merged PR #6 as `3ba489e`; further cross-row accumulation remains deferred because
  it conflicts with tiled/whole bit identity.
- Downloaded and validated all 13 T005 CSLCs plus STATIC; built and ran the MMX1 core and shared
  MMX1/ICMX fixtures with native and SNAPHU.
- Captured the initial SNAPHU pass/native fail, added the red live parity contract, and isolated
  randomized seam ties plus the overly fine 48-pixel floor.
- Committed `18a1652` with deterministic reconciliation, a 64-pixel production floor, contracts,
  and validation documentation.
- Landed reviewed follow-up `9fec550`, including isolated A/B attribution and added deterministic
  vote coverage.
- Closed issues #4 and #5. No GroundPulse submodule bump, release, or deployment occurred.

## In progress

- No code is in progress; the tree is clean.
- The 64-pixel floor's broad 1024x1024 concurrency benchmark remains unrun.
- Atmospheric corrections remain off; live source and result artifacts are local/gitignored.

## Verification

- Corrected live score PASS: truth -104.262 mm; native -94.305 mm; residual +9.958 mm;
  correlation 0.9375; TLS 1.0357; native/SNAPHU endpoint delta 0.000033 mm.
- Final-epoch native/SNAPHU disagreement 0.1918% against the <=0.5% gate.
- Current `9fec550`: `cargo check --workspace`, strict workspace Clippy, and
  `cargo test --workspace` all pass, including GPU and live MMX1 contracts.
- Full GNSS score was not rerun after review-only `9fec550`; atmospheric-corrected and
  GroundPulse integration runs were not performed.

## Open questions

1. Harden provisional GNSS bars now, or wait for more stations and atmosphere?
2. Rebenchmark the 64-pixel floor now, or when a performance decision requires it?
3. When should GroundPulse consume `9fec550` through a human-gated submodule bump?

## Next actions

1. Rebenchmark 64-pixel native tiling before reusing old throughput claims.
2. Decide GNSS gate/atmosphere policy.
3. If desired, bump `../eo` and run GroundPulse worker contracts without conflating that with
   deployment or release.

## References

- Implementation synchronized at `9fec550`; local `main` adds the EOD handoff commit; no open
  PRs/issues.
- Commits: `3ba489e`, `18a1652`, `9fec550`.
- `VALIDATION.md`; `crates/dolphin-unwrap/src/native/tile.rs`;
  `crates/dolphin-unwrap/tests/native_mmx1_live_contract.rs`.
